import { Chapter as DBChapter } from '../database/entity/chapter';

export default interface Chapter extends DBChapter {
    // manga_id: number;
    // url: string;
    // hostname: string;
    // title: string;
    // number: number;
    // posted?: Date;
}
